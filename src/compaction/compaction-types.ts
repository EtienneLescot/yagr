export {
  DEFAULT_COMPACTION_CONFIG,
  buildCompactionContextBlock,
} from '@yagr/session-service';

export type {
  CompactionConfig,
  CompactionState,
  CompactionSubscriber,
  SessionCompactor,
} from '@yagr/session-service';
